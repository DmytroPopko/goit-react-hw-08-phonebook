import React from 'react';
import './AddContact.scss';

const AddContact = () => (
  <div>
    <button type="submit" name="add" className="Contacts__btn">
      Add contact
    </button>
  </div>
);

export default AddContact;
